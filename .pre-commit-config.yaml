repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt --all --check
        entry: cargo fmt --all --check
        language: system
        pass_filenames: false
        files: &rust_workspace_files ^(Cargo\.toml|Cargo\.lock|deny\.toml|clippy\.toml|\.cargo/config\.toml|crates/.*|fuzz/.*|\.github/workflows/.*)$

      - id: cargo-clippy
        name: cargo clippy --workspace --all-targets --all-features
        entry: cargo clippy --workspace --all-targets --all-features -- -D warnings
        language: system
        pass_filenames: false
        files: *rust_workspace_files

      - id: cargo-doc
        name: cargo doc rustdoc broken link check
        entry: sh -c 'RUSTDOCFLAGS="-D rustdoc::broken-intra-doc-links" cargo doc --workspace --all-features --no-deps'
        language: system
        pass_filenames: false
        files: *rust_workspace_files

      - id: no-std-check
        name: cargo check no_std crates
        entry: sh -c 'cargo check -p petkit-types --no-default-features && cargo check -p petkit-protocol --no-default-features'
        language: system
        pass_filenames: false
        files: *rust_workspace_files

      - id: shellcheck
        name: shellcheck scripts/*.sh
        entry: mise exec -- shellcheck
        language: system
        pass_filenames: true
        files: ^scripts/.*\.sh$

      - id: shfmt
        name: shfmt --diff scripts/*.sh
        entry: mise exec -- shfmt --diff -i 2
        language: system
        pass_filenames: true
        files: ^scripts/.*\.sh$

      - id: actionlint
        name: actionlint
        entry: mise exec -- actionlint
        language: system
        pass_filenames: false
        files: ^\.github/workflows/.*\.(yml|yaml)$

      - id: cargo-deny
        name: cargo deny check bans licenses sources
        entry: mise exec -- cargo deny check bans licenses sources
        language: system
        pass_filenames: false
        files: *rust_workspace_files

      - id: cargo-machete
        name: cargo machete
        entry: mise exec -- cargo machete
        language: system
        pass_filenames: false
        files: *rust_workspace_files

      - id: typos
        name: typos
        entry: mise exec -- typos
        language: system
        pass_filenames: false
        files: ^(.*\.(rs|toml|md|yaml|yml|json)|\.github/workflows/.*)$
